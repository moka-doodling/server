<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doodling.admin.mapper.AdminRelayMapper">
    <insert id="insertRelay" parameterType="Relay">
        <![CDATA[
            insert into notice (relay_id, startdate, enddate, title, age, cover)
            values (seq_relay.nextval, #{startdate}, #{enddate}, #{title}, #{age}, #{cover})
        ]]>
	</insert>
</mapper>